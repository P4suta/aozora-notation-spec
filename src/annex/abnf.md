# Annex D. Collected ABNF grammar

This annex collects the normative lexical grammar (§5). Terminals are Unicode
scalar values; the grammar applies to the §4 sanitized source. Body
classification (§6), structural pairing, and forward-reference resolution
(§7) are defined in prose, not in this grammar.

```abnf
{{#include ../grammar/aozora.abnf}}
```
