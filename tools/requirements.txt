# Python dependencies for the spec's validation tools (tools/*.py).
# Pinned loosely; Dependabot (pip ecosystem, /tools) keeps them fresh and
# CI installs from here so host == CI. validate_vectors.py needs jsonschema;
# abnf_check.py and linkcheck.py are pure stdlib.
jsonschema>=4.26.0
